version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo "📦 Instalando dependencias..."
      - npm ci

  build:
    commands:
      - echo "🏗️ Compilando la aplicación NestJS..."
      - npm run build

  post_build:
    commands:
      - echo "✅ Build completado. Empaquetando artefactos..."

artifacts:
  files:
    - '**/*'
  base-directory: dist
